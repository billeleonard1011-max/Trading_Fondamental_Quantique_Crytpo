# Dépendances du socle et du moteur or. Python 3.12.
pandas>=2.2
numpy>=1.26
requests>=2.31
PyYAML>=6.0
feedparser>=6.0
yfinance>=0.2.40
lxml>=5.0
jinja2>=3.1

# Couche pédagogique (modules/gold/explain.py). Facultative à l'exécution :
# sans le paquet ou sans OPENAI_API_KEY, le module bascule sur ses gabarits
# et le reste du système fonctionne à l'identique.
openai>=1.40

# Tests. Le workflow les exécute avant de publier : ils sont donc requis à
# l'exécution en intégration continue, pas seulement en développement.
pytest>=8.0
