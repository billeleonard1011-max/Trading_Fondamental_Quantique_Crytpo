# Dépendances du socle et du moteur or. Python 3.12.
pandas>=2.2
numpy>=1.26
requests>=2.31
PyYAML>=6.0
feedparser>=6.0
yfinance>=0.2.40
lxml>=5.0

# Décompression Brotli. Indispensable et non optionnelle : certains serveurs
# derrière Cloudflare renvoient du « Content-Encoding: br » même lorsque la
# requête n'annonce que gzip et deflate. Sans ce paquet, requests rend alors
# les octets compressés tels quels, feedparser échoue sur « not well-formed
# (invalid token) », et le flux paraît mort alors qu'il fonctionne.
brotli>=1.1
jinja2>=3.1

# Couche pédagogique (modules/gold/explain.py). Facultative à l'exécution :
# sans le paquet ou sans OPENAI_API_KEY, le module bascule sur ses gabarits
# et le reste du système fonctionne à l'identique.
openai>=1.40

# Tests. Le workflow les exécute avant de publier : ils sont donc requis à
# l'exécution en intégration continue, pas seulement en développement.
pytest>=8.0
